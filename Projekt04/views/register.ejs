<%- include('head.ejs') %>
  <div class="container">
    <header class="page-header">
      <h1 class="page-title">Create Account</h1>
    </header>

    <main class="recipe-content">
      <% if (typeof error !== 'undefined' && error) { %>
        <p style="color: red; margin-bottom: 1rem;"><%= error %></p>
      <% } %>

      <form action="/register" method="post" class="recipe-form">
        <label>
          Username
          <input name="username" required type="text" class="form-input" placeholder="Enter username" />
        </label>

        <label>
          Password
          <input name="password" required type="password" class="form-input" placeholder="Enter password" />
        </label>

        <div class="form-buttons">
          <button class="btn" type="submit">Register</button>
          <a class="btn secondary" href="/login">Already have an account? Login</a>
        </div>
      </form>
    </main>
  </div>
<%- include('footer') %>