<%- include('head.ejs') %>
  <div class="container">
    <h1><%= title %></h1>

    <% if (!Array.isArray(recipes) || recipes.length === 0) { %>
      <p>No recipes available.</p>
      <% if (!isLoggedIn) { %>
        <p>Login to add your own recipes!</p>
      <% } %>
    <% } else { %>
      <div class="recipe-list">
        <% recipes.forEach(function(recipe){ %>
          <div class="card">
            <h3><a href="/recipes/<%= encodeURIComponent(recipe.id) %>"><%= recipe.name %></a></h3>
            <p><%= recipe.description %></p>
            <% if (isLoggedIn) { %>
              <div class="card-buttons">
                <form action="/edit/<%= encodeURIComponent(recipe.id) %>" method="GET">
                  <button type="submit" class="btn btn-edit">Edit</button>
                </form>
                <form action="/recipes/<%= encodeURIComponent(recipe.id) %>/delete" method="POST">
                  <button type="submit" class="btn delete">Delete</button>
                </form>
              </div>
            <% } %>
          </div>
        <% }) %>
      </div>
    <% } %>

    <div class="form-buttons" style="justify-content: center; gap: 1rem; margin-top: 2rem;">
      <a class="btn" href="/">Home</a>
      <% if (isLoggedIn) { %>
        <a class="btn secondary" href="/add">Add Recipe</a>
      <% } else { %>
        <a class="btn secondary" href="/login">Login to Add Recipe</a>
      <% } %>
    </div>
  </div>
<%- include('footer') %>